import React from 'react'
import { continents } from '../../Data/Continents'

// let comingSoon = continents.map(continent => {
//     continent.countries = continent.countries.filter(country => country.isEnabled === false)
//     return continent
// }).filter(l => l.countries > 0)

const ComingSoon = () => {
  return (
    <div>

    </div>
  )
}

export default ComingSoon